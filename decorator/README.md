# Decorator pattern example (TypeScript)

## Running the example:
```
node loggable.js
```

Installing the development build chain:
```
npm install
```

Transpiling to Javascript:
```
npm run tsc
```

Transpiling when changes applied to `loggable.ts` (via watcher): 

```npm run watch```